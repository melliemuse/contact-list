// Loads existing contacts from a json-server API, and saves new ones.



ContactCollection = {
    getAPIContactsData : () => {
        return fetch()
    },
    saveAPINewContactData : () => {

    }
}

export default ContactCollection